fastapi>=0.117.1
langfuse>=4.5.1
pydantic-ai-slim[ui]>=2.0.0,<3.0.0
sse-starlette>=3.4.1
starlette>=1.3.1
uvicorn>=0.46.0
watchfiles>=1.1.0
logfire[fastapi,starlette]
# D-WA-1 (reports/deferred/lane-webui-auth.md): agent_utilities.security.auth._decode_jwt
# imports joserfc lazily and turns its absence into a 401 "Token validation failed" that
# looks like a rejected credential, not a missing dependency. Neither this file nor
# pyproject.toml's runtime `dependencies` declares `agent-utilities` at all (it is added at
# image-build time, outside this repo, still pinned there at 1.27.1 -- which gates joserfc
# behind its own since-retired `[auth]` extra that the image build does not request).
# Declared directly here so a fresh image build gets it unconditionally, regardless of which
# agent-utilities version or extras the image build step chooses, instead of depending on the
# out-of-band `/deps` NFS overlay documented in
# inventory/k8s-migration/cutover/apptier/agent-webui.yaml to authenticate a single request.
joserfc>=1.7.4
